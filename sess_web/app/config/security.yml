security:
    firewalls:
        login_firewall:
            pattern: ^/login$
            anonymous: ~
        secured_area:
            pattern: ^/
            anonymous: ~
            form_login:
                login_path: login
                check_path: login_check
    
    access_control:
        - { path: ^/, roles: IS_AUTHENTICATED_ANONYMOUSLY }
    
    providers:
        default_provider:
            memory:
                users:
                    admin: { password: sess2014, roles: "ROLE_ADMIN" }
                